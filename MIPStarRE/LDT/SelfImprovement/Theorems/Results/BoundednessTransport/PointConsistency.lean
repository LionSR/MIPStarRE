import MIPStarRE.LDT.SelfImprovement.Theorems.Results.BoundednessTransport

/-!
# Point-consistency transport compatibility import

The point-consistency transport declarations are exported by
`MIPStarRE.LDT.SelfImprovement.Theorems.Results.BoundednessTransport`.  This
module remains as a compatibility import for older paths; it intentionally
contains no duplicate declarations.
-/
