import MIPStarRE.LDT.Test.ErrorCascade.CascadeBounds.Final

/-!
# Error cascade — bounds and final consolidator

Compatibility module for the Step 8 error-cascade bounds.  The `σ` and `ζ₁`
estimates, the `ζ₂` and `ζ₃` estimates, the `ζ₄` estimate, and the repaired
line-169/final assembly are now separated into reviewable leaf modules under
`CascadeBounds/`.
-/
