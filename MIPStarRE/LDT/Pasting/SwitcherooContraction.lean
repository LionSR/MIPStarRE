import MIPStarRE.LDT.Pasting.SwitcherooContraction.Raw

/-!
# Section 12 pasting: switcheroo contraction lemmas

Compatibility module re-exporting the split switcheroo contraction development.
Declarations live in:
* `SwitcherooContraction.Split` — split-form contraction and first mixed-term transfer
* `SwitcherooContraction.Commuted` — once-commuted contraction steps and split-by-`g` rewrite
* `SwitcherooContraction.Raw` — raw fourth-term transition lemmas
-/
