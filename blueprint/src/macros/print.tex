% Macros for the PDF version only.
% Provides dummy definitions for plasTeX-only commands.

\newcommand{\lean}[1]{}
\newcommand{\discussion}[1]{}
\newcommand{\leanok}{}
\newcommand{\mathlibok}{}
\newcommand{\notready}{}

% Make \uses and \proves check labels (prints warning if undefined)
\ExplSyntaxOn
\NewDocumentCommand{\uses}{m}
 {\clist_map_inline:nn{#1}{\vphantom{\ref{##1}}}%
  \ignorespaces}
\NewDocumentCommand{\proves}{m}
 {\clist_map_inline:nn{#1}{\vphantom{\ref{##1}}}%
  \ignorespaces}
\ExplSyntaxOff

% Home / github / dochome stubs
\newcommand{\home}[1]{}
\newcommand{\github}[1]{}
\newcommand{\ghissue}[1]{}
\newcommand{\dochome}[1]{}