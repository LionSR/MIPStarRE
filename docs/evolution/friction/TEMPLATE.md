# Friction: <one-line summary>

<!--
File name: YYYY-MM-DD-kebab-case-summary.md, where the date is the day the
friction was first noticed (or first noticed as recurring).

Append a row to friction/INDEX.md when adding this report.
-->

- **Filed**: YYYY-MM-DD
- **Filed by**: agent name or username
- **Status**: open | proposed | resolved | dismissed
- **Affects**: which directories / workflows / activities slowed down

## What slowed you down

One paragraph. Concrete: which command, which file, which workflow, what
happened, how much time you estimate it cost.

## How often

How many times have you (or other PRs you have observed) hit this in the
last week / month? Cite PR numbers if available.

## Suggested fix

What change would remove or reduce this friction? Be specific. Examples:

- Add a check in `scripts/<name>.py`.
- Adjust a workflow trigger.
- Split a file or rename a module.
- Document a tactic as a norm under `norms/`.
- Add a snippet to `AGENTS.md`.

If the cause is unknown, say so explicitly and mark the status `open`.

## Resolution

(Filled in when the report moves to `resolved` or `dismissed`. Link the
merging PR or explain the dismissal reason.)
